import L from 'leaflet';
import icon from '../../img/marca.png'

const IconLocation = L.icon({
  iconUrl: icon,
  iconRetinaUrl: icon,
  iconSize: new L.Point(30, 30),
});

export default IconLocation
